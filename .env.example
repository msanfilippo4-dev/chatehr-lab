# FordMS EHR environment (copy to .env.local; never commit .env.local)
NEXTAUTH_SECRET=
NEXTAUTH_URL=https://fordms.com
GOOGLE_CLIENT_ID=
GOOGLE_CLIENT_SECRET=
NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_ANON_KEY=
SUPABASE_SERVICE_ROLE_KEY=
# Comma-separated instructor addresses. The admin role is assigned in the database (migration 009 promotes the course instructor).
INSTRUCTOR_EMAILS=msanfilippo4@fordham.edu

# Test-only sign-in for Playwright. Never set these on a production deployment
# (the app refuses to start with FORDMS_TEST_AUTH=1 when VERCEL_ENV=production).
# FORDMS_TEST_AUTH=1
